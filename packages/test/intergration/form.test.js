import { expect, test, describe } from "vitest";

describe("测试form表单方法", () => {
  test("测试1+1", () => {
    expect(1 + 1).toBe(2);
  });
});

describe("测试form表单方法", () => {
  test("测试1+1", () => {
    expect(1 + 1).toBe(2);
  });
});


describe("测试form表单方法", () => {
  test("测试1+1", () => {
    expect(1 + 1).toBe(2);
  });
});


describe("测试form表单方法", () => {
  test("测试1+1", () => {
    expect(1 + 1).toBe(2);
  });
});

